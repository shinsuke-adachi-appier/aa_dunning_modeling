# Local env (paths, optional secrets); copy from .env.example
.env
# Virtualenv and Python
.venv/
__pycache__/
*.pyc
# Data and logs (too large to push)
data/
logs/
# Optional: uncomment if you don't want to commit these
artifacts/
.env