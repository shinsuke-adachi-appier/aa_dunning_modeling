# Production deployment dependencies.
# Pin to match training environment (see PRODUCTION_DEPLOYMENT_GUIDE).
# timezonefinder + pgeocode for zip-based timezone lookup (optional; country-only works without).

numpy>=1.24,<2
pandas>=2.0
scikit-learn>=1.3,<1.5
catboost>=1.2,<1.3
joblib>=1.3
google-cloud-bigquery>=3.0
google-cloud-storage>=2.0
timezonefinder>=6.0
pgeocode>=0.4
